@require 'utils/*'
@require 'fragments/*'

html, body
    margin 0
    padding 0
    font-family: "kepler-std", script;
    font-weight: 400;
    font-style: normal;
    color white
    background-color: rgb(51,51,51)

body
    width 100%